import { defineConfig } from 'vite';

export default defineConfig({
  // Относительная база: сборка работает и в корне домена, и в подпапке
  // GitHub Pages (https://aumphaadr.github.io/OOM/)
  base: './',
});
